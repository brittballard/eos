<%= form_for(@residual) do |f| %>
  <% if @residual.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@residual.errors.count, "error") %> prohibited this residual from being saved:</h2>

      <ul>
      <% @residual.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :start_month %><br />
    <%= f.text_field :start_month %>
  </div>
  <div class="field">
    <%= f.label :end_month %><br />
    <%= f.text_field :end_month %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
