<%= form_for(@percentage_modifier) do |f| %>
  <% if @percentage_modifier.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@percentage_modifier.errors.count, "error") %> prohibited this percentage_modifier from being saved:</h2>

      <ul>
      <% @percentage_modifier.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
